/**
 * Created by vitaly on 9/12/16.
 */
public class Cycles {
    public static void main(String[] args) {
        for (int i = 0; i < args.length; i++) { // itar + Tab генерит такую штуку
            String arg = args[i];
        }
        for (String arg : args) { // iter + Tab

        }

        int i = 0;
        while (i < args.length) {
            String arg = args[i];
            ++i;
        }

    }
}
